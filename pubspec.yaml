name: better_bots
description: "Better Bots"
publish_to: 'none'
version: 0.1.25+25

environment:
  sdk: ^3.12.1

dependencies:
  chess: ^0.8.1
  flutter:
    sdk: flutter
  flutter_svg: ^2.3.0
  multistockfish: ^0.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/board/maple.jpg
    - assets/pieces/
    - assets/background/
