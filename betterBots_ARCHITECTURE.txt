* 2 TB Lichess Data into Bots effiency creating
* Windows: pfeiltaste oben und unten für komplett stellungsskip
* Windows: markiert jedes Feld über das man beim drag hovered
* correcting premoves
* FEN/PGN Import shoould not be allowing generating wins (cancel wins/looses/remis from imports or fixed positions)

* changing hardcode maia to native code

BUG SECTION:
	* Android: Engine deactivated when tapping backbutton
	(* Android: Laggs by drag shadow view)
	* adding premoves with taking pieces
	* drag should dissapear when holding and clicking on back or forth buttons at the same time


* Opening & Personality Button:
	* multiple selection
	* random selection (random amount)
	* random amount selection
	* renaming "Ohne ..." with "Ohne ... / Auswahl entfernen"


* STRONG: Android: "..." Menü "x" positionierung anpassen
* play from here: setzt die aktuelle Position als Standardpositions
* Android: BackButton calls close request

* challengeModes: timestamps (bullet, blitz, rapid, classic) AND adding count property for black/white + situation(win, loose, remis, train)
* creating own bots with names
(* Analyse, alle existierenden Züge in der Analyse bis Zug 20 analysiert bleibt zwischengespeichert bis Restart oder Reset (oder ResetCount) betätigt wird)
(* Hinter der NotationBar grauen Streifen)


* Windows & Android: darker green for circle markers
* adding also very abstract personality: when having max_cp, lowest_cp then there is more room on move 1-5, less-more room on move 6-10 etc. (name: artist or creative) as Button (standard: 11)
* descending from move1 to move10, after it engine continues

* funnyModes: kingchessNoBorder, botIllegalMoves

* calculating all possible bots including ELO from 1320 to 3190 (100er, 10er and 1er)

* resizing: persona_move_selector.dart
* Kandidatenzüge mathematisch effizienter und qualitativer für die Kreativität ensetzen: z.B. 30 Kanditatenzüge mit Zug 2 und dann absteigend z.B. 25 Kandidatenzüge nach Zug 2

* adding feedback for random functions

* add multiple selection for Openings and Personalities

( * add UCI_ELO 1320 -3190 Bots (10er)
* -1 Elo Bot plays Random

* adding winning and loosing animation-particles

* add restart-count game button
* add "play from position" feature

* mark as favorite Bot feature


* counts the amount of trained games per Bot
* counts the amount of won games per Bot

* Guess the Bot (Online)
